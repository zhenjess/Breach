//sort data by country and records

function mapData(records, year) {

    //associate country with records of incidents
    if (!year || year === "All-time" {
        let dataset = d3.nest()
            .key(function(d) {
                return d.victim.country[4];
            })
    })
}